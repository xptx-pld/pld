# 数据库配置（外部数据库）
DATABASE_URL=mysql+mysqlconnector://recon_user:Recon@2026@mysql.xyyxt.xyz:3306/reconciliation_system

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
DEBUG=false

# JWT配置（请修改为随机字符串）
JWT_SECRET_KEY=dormitory-autonomy-secret-key-2026

# Redis配置（如果没有Redis，留空会跳过）
REDIS_URL=

# 邮件配置（可选）
SMTP_SERVER=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SENDER_EMAIL=
